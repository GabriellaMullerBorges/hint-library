import React from 'react';

class Botao extends React.Component {
    render() {
        return(
            <button>
                Recomendar
            </button>
        )
    }
}

export default Botao;